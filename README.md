# Example Config File

```
# Parallel connections per file
connections = 12

# Max simultaneous downloads in queue mode
max_jobs = 5

# Default download directory
download_dir = "~/Downloads"

# Default queue priority: high | normal | low
default_priority = "normal"
```
